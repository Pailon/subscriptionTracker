# 🥧 Инструкция по деплою на Raspberry Pi

## ✅ Исправлена ошибка Docker

**Проблема**: `failed to compute cache key: "/app/drizzle": not found`

**Решение**: Удалена строка копирования несуществующей папки `drizzle` из Dockerfile

## 🚀 Команды для деплоя

На Raspberry Pi выполните:

```bash
# 1. Обновите код из репозитория
cd ~/subscriptionTracker
git pull

# 2. Пересоберите Docker образы
docker-compose down
docker-compose build --no-cache

# 3. Запустите контейнеры
docker-compose up -d

# 4. Проверьте статус
docker-compose ps

# 5. Проверьте логи
docker-compose logs -f backend

# 6. Примените миграции базы данных (если нужно)
# Примечание: миграции нужно запускать локально перед деплоем
# или подключиться к контейнеру с установленными dev-зависимостями
```

## 📋 Проверка работоспособности

```bash
# Проверка health endpoint
curl http://localhost:3000/health

# Должен вернуть:
# {"status":"ok","timestamp":"2025-11-07T..."}
```

## 🔧 Если возникли проблемы

### Очистка Docker кеша

```bash
docker system prune -a
docker-compose build --no-cache
docker-compose up -d
```

### Проверка переменных окружения

```bash
cat .env
```

Убедитесь, что все переменные заполнены:
- `DATABASE_URL`
- `TELEGRAM_BOT_TOKEN`
- `FRONTEND_URL`
- `WEBHOOK_URL`

### Проверка подключения к БД

```bash
docker-compose exec postgres psql -U postgres -d subscription_tracker -c "SELECT NOW();"
```

## 📊 Мониторинг

```bash
# Логи backend
docker-compose logs -f backend

# Логи PostgreSQL
docker-compose logs -f postgres

# Использование ресурсов
docker stats
```

## 🔄 Обновление после изменений в коде

```bash
git pull
docker-compose up -d --build
```

## ✅ Готово!

После успешного запуска backend будет доступен на `http://localhost:3000`

