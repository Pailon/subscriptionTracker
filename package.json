{
  "name": "telegram-subscription-tracker",
  "version": "1.0.0",
  "description": "Telegram Mini App для отслеживания подписок",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w backend && npm run build -w frontend",
    "start": "npm run start -w backend"
  },
  "keywords": [
    "telegram",
    "mini-app",
    "subscriptions",
    "tracker"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

